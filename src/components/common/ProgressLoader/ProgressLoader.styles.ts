import { styled } from "@mui/material/styles";
import { Box } from "@mui/material";

export const ProgressLoaderContainer = styled(Box)({
  width: "100%",
  position: "absolute",
  bottom: 0,
  left: 0,
  right: 0,
});
